<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nl.hhs.werkbon.werkbon" >

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat" >
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize|stateVisible" >
        </activity>
        <activity
            android:name=".WorkOrderListActivity"
            android:label="@string/title_activity_work_order_list" >
        </activity>
        <activity
            android:name=".WorkOrderDetailActivity"
            android:label="@string/title_activity_work_order_detail" >
        </activity>
        <activity
            android:name=".WorkOrderDetailTabbedActivity"
            android:label="@string/title_activity_work_order_detail_tabbed" >
        </activity>
    </application>

</manifest>
